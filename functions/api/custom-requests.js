const out=(d,s=200)=>Response.json(d,{status:s,headers:{'Cache-Control':'no-store'}}),clean=(v,n=1000)=>String(v??'').trim().slice(0,n),allowed=new Set(['rating','personal_video','story','story_narrated','roleplay_kink','other']);
export async function onRequestPost({request,env}){try{const b=await request.json();if(clean(b.website))return out({ok:true,reference:'KH-CUSTOM'});const name=clean(b.guest_name,100),contact=clean(b.contact_value,200),brief=clean(b.brief,2000),type=allowed.has(b.request_type)?b.request_type:'other';if(!name||!contact||!brief)throw Error('Please complete your name, contact details and request.');if(!b.terms)throw Error('Please confirm that you understand the request terms.');const ref=`KH-C${crypto.randomUUID().slice(0,6).toUpperCase()}`;await env.DB.prepare(`INSERT INTO custom_requests(reference,guest_name,contact_method,contact_value,request_type,format_details,personal_name,brief,budget_band,deadline) VALUES(?,?,?,?,?,?,?,?,?,?)`).bind(ref,name,clean(b.contact_method,20),contact,type,clean(b.format_details,300),clean(b.personal_name,80),brief,clean(b.budget_band,30),clean(b.deadline,10)||null).run();return out({ok:true,reference:ref},201)}catch(e){return out({error:e.message||'Your request could not be submitted.'},400)}}
