(()=>{const form=document.querySelector('#booking-request-form'),status=document.querySelector('#form-status'),phone=document.querySelector('#phone-field'),passwords=document.querySelector('#password-fields');function mode(){const privateRoom=form.elements.contact_preference.value==='private_house_reply';passwords.hidden=!privateRoom;phone.hidden=privateRoom;form.elements.phone.required=!privateRoom;form.elements.private_password.required=privateRoom;form.elements.private_password_confirm.required=privateRoom}form.querySelectorAll('[name=contact_preference]').forEach(r=>r.addEventListener('change',mode));form.addEventListener('submit',async e=>{e.preventDefault();status.hidden=true;const button=form.querySelector('button[type=submit]');button.disabled=true;button.textContent='SENDING…';try{const data=Object.fromEntries(new FormData(form));data.rates_read=Boolean(form.elements.rates_read.checked);data.deposit_agreed=Boolean(form.elements.deposit_agreed.checked);const r=await fetch('/api/booking-requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}),x=await r.json();if(!r.ok)throw Error(x.error||'Your request could not be sent.');form.innerHTML=`<section class="success"><h2>Your request has entered the House.</h2><p>Keep this booking reference: <b>${x.reference}</b>.</p>${x.anonymous?`<p>Save your reference and private password. If you leave, return through <b>thehouseofkalli.com → Returning Clients → Return to a Booking</b>.</p><a href="booking-room.html?token=${encodeURIComponent(x.token)}">ENTER YOUR WAITING ROOM</a><a href="booking-return.html">RETURN LATER</a>`:'<p>Kalli will review it and contact you if she wishes to proceed.</p>'}</section>`}catch(err){status.textContent=err.message;status.hidden=false}finally{button.disabled=false;button.textContent='SEND MY BOOKING REQUEST'}})})();
